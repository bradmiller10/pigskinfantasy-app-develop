import React from "react";
import { Text, View } from "react-native";
import { ScreenComponent } from "@/typings";

export const Admin: ScreenComponent = ({ navigation }) => {
  return (
    <View>
      <Text>Admin</Text>
    </View>
  );
};
