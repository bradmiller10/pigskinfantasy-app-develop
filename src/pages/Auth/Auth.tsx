import React from "react";
import { Text, View } from "react-native";
import { ScreenComponent } from "@/typings";

export const Auth: ScreenComponent = ({ navigation }) => {
  return (
    <View>
      <Text>Login</Text>
    </View>
  );
};
