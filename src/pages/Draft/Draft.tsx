import React from "react";
import { Text, View } from "react-native";
import { ScreenComponent } from "@/typings";

export const Draft: ScreenComponent = ({ navigation }) => {
  return (
    <View>
      <Text>Draft</Text>
    </View>
  );
};
