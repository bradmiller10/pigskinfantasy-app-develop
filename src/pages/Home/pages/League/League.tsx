import React from "react";
import { Text, View } from "react-native";
import { ScreenComponent } from "@/typings";

export const League: ScreenComponent = ({ navigation }) => {
  return (
    <View>
      <Text>L:eagu</Text>
    </View>
  );
};
