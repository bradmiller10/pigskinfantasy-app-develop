import React from "react";
import { Text, View } from "react-native";
import { ScreenComponent } from "@/typings";

export const Profile: ScreenComponent = ({ navigation }) => {
  return (
    <View>
      <Text>Profile</Text>
    </View>
  );
};
