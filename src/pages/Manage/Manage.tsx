import React from "react";
import { Text, View } from "react-native";
import { ScreenComponent } from "@/typings";

export const Manage: ScreenComponent = ({ navigation }) => {
  return (
    <View>
      <Text>Manage</Text>
    </View>
  );
};
